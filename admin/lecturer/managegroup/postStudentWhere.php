<?php


   //connect to database
   $conn = mysqli_connect('localhost', 'root', '', 'grouping_system');

   //Check for post variables now the now the qquestion is who is going to maek shtat post and what kind of data is going to get sent to us?
   if(isset($_POST[]))
   {
       
   }
